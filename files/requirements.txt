# Install torch via AMD's ROCm wheels or (preferred) use the ROCm PyTorch Docker image:
#   rocm/pytorch:rocm6.4_ubuntu22.04_py3.10_pytorch_release_2.6.0
# torch / torchvision: provided by the ROCm image — do not pip a CUDA build over it.
monai>=1.4
nibabel>=5.3
numpy>=2.0
scipy>=1.13
tqdm>=4.67
# optional fusion meta-ranker (use Apache-2.0 v2 weights): tabpfn
